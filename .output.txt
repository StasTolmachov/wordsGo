@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Spectral:wght@500;600&display=swap');
:root {
    color-scheme: light;
    --bg: #f7f4ef;
    --surface: #ffffff;
    --ink: #1d1b16;
    --muted: #6f6a5f;
    --accent: #e36f4c;
    --accent-dark: #c45b3c;
    --border: #e3ddd3;
    --shadow: 0 18px 40px rgba(24, 22, 18, 0.08);
}
* {
    box-sizing: border-box;
}
body {
    margin: 0;
    font-family: 'Space Grotesk', 'Segoe UI', sans-serif;
    color: var(--ink);
    background: radial-gradient(circle at top left, #fff2d9, var(--bg));
}
a {
    color: inherit;
    text-decoration: none;
}
.app {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}
.topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 20px 32px;
    background: rgba(255, 255, 255, 0.8);
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    backdrop-filter: blur(10px);
    z-index: 2;
}
.brand {
    display: flex;
    align-items: center;
    gap: 12px;
    font-weight: 600;
}
.logo {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: var(--accent);
    color: #fff;
    border-radius: 12px;
    font-weight: 700;
}
.brand-text {
    font-size: 1.1rem;
}
.nav {
    display: flex;
    gap: 18px;
    font-size: 0.95rem;
}
.nav a {
    padding: 6px 10px;
    border-radius: 999px;
}
.nav a:hover {
    background: #f0ece6;
}
.main {
    flex: 1;
    display: flex;
    justify-content: center;
    padding: 32px 16px 60px;
}
.card {
    width: min(840px, 100%);
    background: var(--surface);
    border-radius: 24px;
    padding: 32px;
    box-shadow: var(--shadow);
    border: 1px solid var(--border);
}
.title {
    font-family: 'Spectral', serif;
    margin: 0 0 12px;
    font-size: 2rem;
}
.form {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-top: 16px;
}
.form.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 12px;
    align-items: end;
}
.label {
    font-size: 0.85rem;
    color: var(--muted);
}
.input {
    padding: 10px 12px;
    border-radius: 12px;
    border: 1px solid var(--border);
    font-size: 1rem;
    background: #fdfbf8;
}
.btn {
    border: none;
    border-radius: 999px;
    padding: 10px 18px;
    font-weight: 600;
    cursor: pointer;
}
.btn.primary {
    background: var(--accent);
    color: #fff;
}
.btn.primary:hover {
    background: var(--accent-dark);
}
.btn.ghost {
    background: transparent;
    border: 1px solid var(--border);
}
.btn.danger {
    background: #f2d3ce;
    color: #7c2f25;
}
.row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
}
.list {
    margin-top: 20px;
    display: flex;
    flex-direction: column;
    gap: 12px;
}
.list-item {
    padding: 12px 16px;
    border-radius: 16px;
    border: 1px solid var(--border);
    background: #fffdf9;
}
.chip-row {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 16px;
}
.chip {
    padding: 6px 14px;
    border-radius: 999px;
    border: 1px solid var(--border);
    background: #f7f2ea;
    font-weight: 600;
}
.chip.small {
    font-size: 0.75rem;
}
.muted {
    color: var(--muted);
}
.small {
    font-size: 0.8rem;
}
.error {
    color: #b04734;
    font-weight: 600;
}
.success {
    color: #2f6b4f;
    font-weight: 600;
}
.warning {
    color: #8c5a2b;
    font-weight: 600;
}
.divider {
    height: 1px;
    background: var(--border);
    margin: 24px 0;
}
.lesson-container {
    display: flex;
    flex-direction: column;
    gap: 20px;
    align-items: center;
}
.lesson-header {
    width: min(520px, 100%);
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
}
.lesson-state {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
}
.stats {
    display: flex;
    gap: 16px;
}
.stat {
    font-weight: 600;
}
.stat.success {
    color: #2f6b4f;
}
.stat.danger {
    color: #b04734;
}
.icon {
    width: 16px;
    height: 16px;
}
.spinner {
    width: 40px;
    height: 40px;
    color: var(--accent);
    animation: spin 1s linear infinite;
}
.word-card {
    width: min(520px, 100%);
    border-radius: 24px;
    border: 1px solid var(--border);
    background: #fff;
    box-shadow: var(--shadow);
    padding: 20px;
}
.word-card-inner {
    min-height: 320px;
    display: flex;
    align-items: center;
    justify-content: center;
}
.card-face {
    width: 100%;
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 16px;
    align-items: center;
    justify-content: center;
}
.card-face.back {
    background: #fbf7f1;
    border-radius: 20px;
    padding: 24px;
}
.eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.7rem;
    color: var(--muted);
    font-weight: 600;
}
.word {
    font-size: 2.4rem;
    margin: 0;
}
.word.small {
    font-size: 1.6rem;
}
.translation {
    font-size: 2rem;
    color: var(--accent-dark);
    margin: 0;
    font-weight: 600;
}
.bar {
    width: 48px;
    height: 4px;
    background: var(--accent);
    border-radius: 999px;
}
@keyframes spin {
    from {
        transform: rotate(0deg);
    }
    to {
        transform: rotate(360deg);
    }
}
@media (max-width: 720px) {
    .topbar {
        flex-direction: column;
        align-items: flex-start;
        gap: 16px;
    }
    .nav {
        flex-wrap: wrap;
    }
    .card {
        padding: 24px;
    }
}
